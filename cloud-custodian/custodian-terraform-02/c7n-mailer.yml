queue_url: https://sqs.ap-northeast-2.amazonaws.com/${ACCOUNT_ID}/custodian-notify-queue
role: arn:aws:iam::${ACCOUNT_ID}:role/c7n-mailer-role
region: ap-northeast-2

slack:
  default_emoji: ":rotating_light:"
  default_channel: "#security-alerts" 
  good: ${GOOD_SLACK}
  warning: ${WARNING_SLACK}
  danger: ${DANGER_SLACK}

template_dir: templates
